export type LegacyRecord={completed_nodes?:Array<string|{node_id:string}>;xp?:number;rarity?:string;resumeSectionId?:string;canonicalCardIds?:string[]};
export type ReconciliationResult={importedCompletion:boolean;ignoredGameFields:string[];unresolvedMappings:string[];unresolvedAssets:string[];exceptionsRequiringReview:string[];resumeSectionId?:string;cardAlreadyOwned:boolean};
const validSections=new Set(['section.uruk.masthead','section.uruk.opening-city-question','section.uruk.water-food-and-labor','section.uruk.the-built-city','section.uruk.tablets-and-administration','section.uruk.evidence-and-reconstruction','section.uruk.connections','section.uruk.check-and-complete']);
export function reconcileLegacyUruk(records:LegacyRecord[]):ReconciliationResult{const nodes=new Set(records.flatMap(r=>r.completed_nodes??[]).map(n=>typeof n==='string'?n:n.node_id));const ignored=[...new Set(records.flatMap(r=>[r.xp!==undefined?'xp':'',r.rarity!==undefined?'rarity':'']).filter(Boolean))];const resume=records.map(r=>r.resumeSectionId).find((x):x is string=>!!x);return{importedCompletion:nodes.has('uruk'),ignoredGameFields:ignored,unresolvedMappings:[...nodes].filter(x=>x!=='uruk'),unresolvedAssets:['media.uruk.candidate: provenance review required'],exceptionsRequiringReview:resume&&!validSections.has(resume)?[`invalid resume section: ${resume}`]:[],resumeSectionId:resume&&validSections.has(resume)?resume:undefined,cardAlreadyOwned:records.some(r=>r.canonicalCardIds?.includes('card.place.uruk'))}}
